apiVersion: v1
kind: Service
metadata:
  name: momo-db
  namespace: db
spec:
  selector:
    app: momo-db
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  type: ClusterIP
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: momo-db
  namespace: db
  labels:
    app: momo-db
spec:
  serviceName: momo-db
  replicas: 1
  selector:
    matchLabels:
      app: momo-db
  template:
    metadata:
      labels:
        app: momo-db
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: momo_db
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: momo-db-secrets
                  key: username
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: momo-db-secrets
                  key: password
          volumeMounts:
            - name: postgres-data
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata:
        name: postgres-data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 512Mi
